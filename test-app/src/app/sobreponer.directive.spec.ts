import { SobreponerDirective } from './sobreponer.directive';

describe('SobreponerDirective', () => {
  it('should create an instance', () => {
    const directive = new SobreponerDirective();
    expect(directive).toBeTruthy();
  });
});
